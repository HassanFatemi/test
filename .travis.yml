sudo: required
services:
  - docker

script:
  - docker build -t HassanFatemi/node .
  - docker images HassanFatemi/node 

before_deploy:
  - docker login -u 'smartdatasolutions' -p '#Dk122616053'

deploy:
  provider: script
  script: docker push HassanFatemi/node
  on:
    branch: master